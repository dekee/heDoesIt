apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hedoesit-ingress
  namespace: hedoesit
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  ingressClassName: traefik
  rules:
    - host: hedoesit.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80
